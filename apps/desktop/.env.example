# Base URL of the AssessX API. Development uses .env.development (committed, localhost);
# production builds read .env.production (see .env.production.example). Never put secrets here:
# everything in a Vite env file is embedded in the client.
VITE_API_BASE_URL=http://127.0.0.1:8000
