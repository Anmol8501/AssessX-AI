# Copy to .env.production before `npm run build` / `npm run tauri:build`.
# The production API origin must also be allowed in src-tauri/tauri.conf.json (CSP connect-src).
VITE_API_BASE_URL=https://api.example.org
